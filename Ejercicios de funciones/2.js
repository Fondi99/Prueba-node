//Crear una función que reciba por teclado un número y luego informar por pantalla con un cartel aclaratorio si el mismo es par o impar
function parOImpar(n){
    if (n%2==0){
        console.log(`El numero ${n} es par`)
    }else {
        console.log(`El numero ${n} es impar`)
    }
}